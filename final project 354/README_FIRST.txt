we had pip installed Flask, Flask-MYSQLdb, Flask-WTF, and PyYaml. We used Mysql.
To connect your database properly in db_config.yaml make sure to use the sql dump and 
make sure the database name matches . Make sure all data in the db_config.yaml corresponds to your DBMS.